<div class="<%= content %>">
  <h2 class="font-bold text-3xl text-500"><%= @counter %>. <%= @item.title %></h2>
  <h4><%= number_to_currency(@item.price, :unit => "€") %></h4>
  <h4><%= @item.location %></h4>
  <p><a href='#'> <%= @notice %> </a></p>

  <%= render(DiscountComponent.new(item: @item)) %>
  <%= EnrolleeComponent.new.total_enrollees(@item) %>
</div>
